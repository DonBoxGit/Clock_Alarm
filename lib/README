Timer_Blink library organizes the time delay and the time switch state.
To create an object, need to apply the constructor the Timer and as 
parameter to point time as milliseconds.
For example:
  Timer displayShowTimer(2000);